{
  "name": "@hayamiya14/demo-github-actions",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "hayamiya",
  "type": "module",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "import": "./lib/index.js"
    }
  },
  "module": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "check": "npm run lint && npm test && npm run check:package-name && npm run build",
    "check:package-name": "node scripts/check-package-name.cjs",
    "clean": "rimraf lib",
    "format": "prettier --write . && npx sort-package-json",
    "lint": "tsc --noEmit && prettier --check .",
    "prepare": "npm run build",
    "rebuild": "npm run clean && npm run build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest --watch"
  },
  "config": {
    "default": "template",
    "scope": "@hayamiya14/"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.2",
    "prettier": "^3.5.3",
    "prettier-plugin-organize-imports": "^4.1.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
